# Conventions

## Paperclip
- TypeScript strict mode.
- Monorepo structure managed by `pnpm-workspace.yaml`.
- Functional and modular design with embedded database (no external infra required).

## Nanobot
- Python 3.11+ modern async programming.
- Uses `pyproject.toml` for dependency management.
- Simple, readable agent core without deep abstractions.
