---
phase: 01-workspace-foundation-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
  - docs/integration/paperclip-nanobot-bridge.md
  - docs/decisions/0001-control-plane-worker-boundary.md
  - scripts/smoke-integrated.sh
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Contributors can start from the repository root and understand how Paperclip and Nanobot fit together in one project.
    - The repository contains a written control-plane-to-worker contract that defines payload shape, lifecycle states, and ownership boundaries.
    - The root workspace exposes a repeatable smoke-test path for the combined Node.js and Python development setup.
  artifacts:
    - path: README.md
      provides: Root-level project overview, quickstart, and workspace map for the integrated platform
    - path: package.json
      provides: Root scripts that delegate into Paperclip and Nanobot setup or smoke-test flows
    - path: docs/integration/paperclip-nanobot-bridge.md
      provides: First bridge contract for dispatch, task metadata, lifecycle, and result reporting
    - path: docs/decisions/0001-control-plane-worker-boundary.md
      provides: ADR recording why Paperclip owns orchestration while Nanobot owns worker execution
    - path: scripts/smoke-integrated.sh
      provides: A root-level smoke-test entrypoint for verifying the integrated workspace assumptions
  key_links:
    - from: README.md
      to: docs/integration/paperclip-nanobot-bridge.md
      why: The root quickstart must point engineers to the contract they are implementing in later phases.
    - from: package.json
      to: scripts/smoke-integrated.sh
      why: Root developer commands should expose the smoke-test flow without requiring manual path discovery.
    - from: docs/decisions/0001-control-plane-worker-boundary.md
      to: docs/integration/paperclip-nanobot-bridge.md
      why: The bridge contract must follow the documented orchestration ownership boundaries.
---

<objective>
Create the root workspace foundation for the combined autonomous-agency platform.

Purpose: Lock the repo shape, documentation, and bridge contract before runtime integration work begins.
Output: Root onboarding artifacts, a control-plane/worker ADR, a first bridge contract doc, and a root smoke-test entrypoint.
</objective>

<execution_context>
@/Users/atif/Public/Project 2/.codex/get-shit-done/workflows/execute-plan.md
@/Users/atif/Public/Project 2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workspace-foundation-and-contracts/01-CONTEXT.md
@paperclip/AGENTS.md
@paperclip/README.md
@paperclip/package.json
@nanobot/README.md
@nanobot/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root onboarding and workspace command surface</name>
  <files>README.md, package.json</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-workspace-foundation-and-contracts/01-CONTEXT.md, paperclip/README.md, paperclip/package.json, nanobot/README.md, nanobot/pyproject.toml</read_first>
  <action>Create a new root `README.md` that explains the combined platform in brownfield terms: what the product is, why `paperclip/` and `nanobot/` both exist, which system owns orchestration, and the recommended root-first developer journey. Include a short workspace map, prerequisites, and "first commands" that delegate into the two subprojects without pretending the runtime bridge already exists. Create a root `package.json` that is documentation-oriented but still useful, with scripts such as `dev:paperclip`, `dev:nanobot`, and `smoke` that call into the existing subprojects or the new smoke script. Do not invent a fake monorepo build; keep scripts thin and explicit so Phase 1 stays truthful.</action>
  <verify>test -f README.md && test -f package.json && node -e "const pkg=require('./package.json'); console.log(['dev:paperclip','dev:nanobot','smoke'].every((k)=>pkg.scripts&&pkg.scripts[k]))"</verify>
  <acceptance_criteria>
    - `README.md` contains the phrases `Paperclip`, `Nanobot`, and `control plane`
    - `README.md` includes sections for workspace structure and local development
    - `package.json` defines `dev:paperclip`, `dev:nanobot`, and `smoke` scripts
  </acceptance_criteria>
  <done>Root onboarding exists and a contributor can discover the main commands from the repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Write the control-plane boundary ADR and first bridge contract</name>
  <files>docs/decisions/0001-control-plane-worker-boundary.md, docs/integration/paperclip-nanobot-bridge.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-workspace-foundation-and-contracts/01-CONTEXT.md, paperclip/AGENTS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `docs/decisions/0001-control-plane-worker-boundary.md` as a concise ADR that records the brownfield decision: Paperclip owns task lifecycle, approvals, budgets, and audit history; Nanobot owns worker execution internals; peer-to-peer agent messaging is out of scope; channel-heavy Nanobot surfaces are not part of the v1 content-agency path. Then create `docs/integration/paperclip-nanobot-bridge.md` that defines the first contract for later implementation: initiating actor, dispatch trigger, minimum payload fields, lifecycle states, success payload, failure payload, idempotency or correlation expectations, and which side persists which data. Keep the contract implementation-agnostic enough for Phase 2, but concrete enough that later code can be tested against it.</action>
  <verify>test -f docs/decisions/0001-control-plane-worker-boundary.md && test -f docs/integration/paperclip-nanobot-bridge.md && rg -n \"lifecycle|payload|Paperclip|Nanobot|audit|correlation\" docs/decisions/0001-control-plane-worker-boundary.md docs/integration/paperclip-nanobot-bridge.md</verify>
  <acceptance_criteria>
    - `docs/decisions/0001-control-plane-worker-boundary.md` states that Paperclip is the orchestration source of truth
    - `docs/integration/paperclip-nanobot-bridge.md` lists required request fields and lifecycle states
    - The bridge contract explicitly says direct peer-to-peer agent communication is not part of this integration path
  </acceptance_criteria>
  <done>The repo has a durable contract that future bridge code can implement without re-deciding ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Add a root smoke-test entrypoint for the integrated workspace</name>
  <files>scripts/smoke-integrated.sh, package.json, README.md</files>
  <read_first>paperclip/AGENTS.md, paperclip/package.json, nanobot/pyproject.toml, .planning/codebase/TESTING.md</read_first>
  <action>Create `scripts/smoke-integrated.sh` as a bash script that checks the minimum brownfield assumptions without requiring a full bridge implementation yet. It should validate the presence of the Paperclip and Nanobot project files, print the expected local boot sequence, and run cheap non-destructive checks such as showing the Paperclip package scripts and confirming Nanobot's Python project metadata exists. Update the root `package.json` `smoke` script to call this file, and update `README.md` so the smoke-test path is part of the root onboarding flow. Keep the script honest: report missing prerequisites clearly and exit non-zero when core workspace assumptions fail.</action>
  <verify>bash scripts/smoke-integrated.sh && npm run smoke --silent</verify>
  <acceptance_criteria>
    - `scripts/smoke-integrated.sh` exits non-zero if `paperclip/package.json` or `nanobot/pyproject.toml` is missing
    - `package.json` `smoke` script invokes `scripts/smoke-integrated.sh`
    - `README.md` documents the smoke-test command from the repository root
  </acceptance_criteria>
  <done>The root workspace exposes a repeatable and truthful smoke-test path for this integration project.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f README.md && test -f package.json`
- [ ] `test -f docs/decisions/0001-control-plane-worker-boundary.md && test -f docs/integration/paperclip-nanobot-bridge.md`
- [ ] `bash scripts/smoke-integrated.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No misleading "fully integrated" claims are introduced before the bridge exists
- Phase 1 artifacts give later phases one clear contract and one clear root developer entrypoint

</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-foundation-and-contracts/01-01-SUMMARY.md`
</output>
